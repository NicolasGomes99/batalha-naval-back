meta {
  name: Logout
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}

script:post-response {
  // bru.setEnvVar("accessToken", "");
}

settings {
  encodeUrl: true
  timeout: 0
}
